# ============================================================
# 换行标准：本仓库所有文本文件一律使用 LF
# ------------------------------------------------------------
# 这条规则写在仓库里（而不是某台机器的 git config 里），
# 因此优先级高于 core.autocrlf —— 任何机器、任何编辑器都压不过它。
# 文档：https://git-scm.com/docs/gitattributes
# ============================================================

# 让 git 自动判断哪些是文本文件；是文本的，工作区一律写成 LF
* text=auto eol=lf

# ------------------------------------------------------------
# 以下为二进制文件，明确声明不参与任何换行转换。
# 必须写在上面的 * 规则之后 —— 后面的规则覆盖前面的。
# binary 是 git 内置宏，等价于 -text -diff
# ------------------------------------------------------------
*.png  binary
*.jpg  binary
*.jpeg binary
*.webp binary
*.gif  binary
*.ico  binary
*.jfif binary

# ------------------------------------------------------------
# 例外：Windows 批处理文件
# ------------------------------------------------------------
# .bat / .cmd 用纯 LF 在少数情况下会出问题（goto 跳转、老版本 cmd）。
# 所以这两类单独规定：仓库里照旧存 LF，但检出到磁盘时写成 CRLF。
# 项目当前没有这类文件 —— 规则先放好，以后加了自动生效。
*.bat text eol=crlf
*.cmd text eol=crlf
